---
'@gabrielbryk/json-schema-to-zod': minor
---

- Add support for JSON Schema `dependentRequired` on objects with optional custom error message.
- Extend format handling and add bigint format helpers while warning on unknown string formats.
